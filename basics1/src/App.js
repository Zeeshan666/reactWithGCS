import logo from './logo.svg';
import './App.css';

function App() {
  return (
    // You can only return one tag => if you want to return more enclose it in <> </> => this is called fragments
    // Secondly function name should start with capital letter
    // if html returned use .jsx extension if only js use .js
    
    <h1>Hello</h1>
  );
}

export default App;
 // rendering done by react-script